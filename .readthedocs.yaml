version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    build:
      html:
        # -c 指定包含 conf.py 的目录
        # 最后一个参数 docs/source 是源文件目录
        - sphinx-build -c docs/source -b html docs/source/ $READTHEDOCS_OUTPUT/html
